# OSSDrive 文件中转技能

## 目标

使用公开的 OSSDrive 网盘在本地文件与远端 OSS 之间中转文件。服务地址：

`http://f.axe3.cn:5000/`

这是一个免注册、免登录、无需 API Token 的公开网页。不要向用户索取账号、密码或 OSS 密钥。

## 浏览器操作

优先使用浏览器访问网盘页面，不要猜测或生成不存在的 API。

### 上传文件

1. 打开 `http://f.axe3.cn:5000/`。
2. 点击上传区域“点击或拖拽文件上传”，在文件选择器中选择本地文件；或者把文件拖入该区域。
3. 等待上传区域显示“上传中 xx%”并完成。
4. 上传完成后，等待列表自动刷新；页面也会每 10 秒自动刷新。
5. 上传过程中不要重复点击上传区域，避免重复生成上传任务。

上传数据由浏览器直接发送到 OSS，不经过网盘应用服务器。上传地址请求按客户端 IP 限制频率，每分钟最多 10 次。

### 目录

- 点击文件夹名称进入目录。
- 进入目录后再上传，文件会自动保存到当前目录。
- 使用“返回上级”返回父目录。
- 使用“新建目录”在当前目录创建子目录。
- 目录名只能使用中文、英文、数字、短横线和下划线，长度为 1-64 个字符；不能包含空格、斜杠或其他特殊字符。

### 下载文件

在文件列表对应行点击“下载”。网盘会生成临时下载地址并开始下载。大文件下载时应等待浏览器显示完成，不要重复点击。

### 删除文件

在文件列表对应行点击“删除”，确认后删除文件。删除是公开操作，只有在用户明确要求时执行；删除前向用户确认目标文件名。删除后列表会自动刷新。

## 交互约定

- 网盘页面是唯一面向用户的操作入口。
- 文件名、目录名必须使用用户实际提供的名称，不要擅自改名。
- 不要把 OSS 密钥、签名 URL 或内部服务配置写入回复、日志、技能文件或代码。
- 不要把此技能文本当作用户任务本身；它只描述如何操作 OSSDrive。
- 如果上传失败，读取页面上的错误提示并向用户报告，不要反复快速重试。
